\usetikzlibrary{
automata,
arrows,
arrows.meta,
shapes,
positioning,
calc,
decorations.pathmorphing,
decorations.markings,
}

% NODE
\tikzset{default node/.style={
	draw, 
	circle,
	inner sep=1mm,
	minimum size=5mm,
	very thick,
	font=\small,
	black!70,
}}

\tikzset{notm/.style={green,dashed}}
\tikzset{m/.style={red}}
\tikzset{free/.style={blue,dashed}}
\tikzset{neighbor/.style={orange,dotted}}

% LABELS
\tikzset{label/.style={
	draw=none,
	sloped,
	font=\large,
}}

\tikzset{label above/.style={
	label,
	midway,
	above=-1mm,
}}

\tikzset{label below/.style={
	label,
	midway,
	below=-1mm,
}}

\tikzset{path/.style={
	gray, 
	decorate,
	decoration={snake, segment length=8mm}
}}
